
- Indentation is important. If its indented means its within for loop
- **Random.choice(list)** select the random values from the list but it does not enforce uniqueness
- **Random.shuffle(lis)** is used to randomize the values from the list.
- Range in the list do not consider the last number, hence (1, nr_letters + 1) or (0, nr_letter)